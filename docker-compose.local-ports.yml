services:
  backend:
    ports:
      - "${AFFIRM_BACKEND_PORT:-8100}:8000"

  frontend:
    ports:
      - "${AFFIRM_FRONTEND_PORT:-4000}:3000"

  minio:
    ports:
      - "${AFFIRM_MINIO_PORT:-9100}:9000"
      - "${AFFIRM_MINIO_CONSOLE_PORT:-9101}:9001"
